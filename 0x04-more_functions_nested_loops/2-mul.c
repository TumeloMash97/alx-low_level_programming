#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: first integer to multiply
 * @b: Second integer to multiply with first
 * Return: multiplication results of a and b
 */

int mul(int a, int b)
{
return (a * b);
}
